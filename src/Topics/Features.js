import React from "react";

const Features = () => {
  return (
    <div>
      <h1 className="text-center mb-3">Features of Mirco-Frontends</h1>
      <ul className="h2">
        <li>
          Each frontend represents a specific feature or subdomain of the entire
          application
        </li>
        <li>Each frontend can be implemented with a separate team.</li>
        <li>Each frontend can be implemented with different technology.</li>
        <li>Each Frontend can be owned by a single team.</li>
      </ul>
    </div>
  );
};

export default Features;
